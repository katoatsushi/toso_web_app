<h1><%= @user.name %>さん、ようこそ</h1>

<div class="container">
    <div class="row">
    	<% @my_buildings.each do |b| %>
        <!-- <div class="col-xs-3"> -->
        <div class="col-xs-6 col-md-3">
        <div class = 'building_document'>
			<%= link_to  building_path(b.id) do %>

				<%= b.building_name %>
				<div>
					<%= b.user.name %>さんの資料
			    </div>
	    	<% end %>
		</div>
	    </div>
	    <% end %>
    </div>
</div>
